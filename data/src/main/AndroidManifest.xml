<manifest package="com.transition.scorekeeper.data"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">

        <provider
            android:name=".cache.database.provider.BaseProvider"
            android:authorities="com.transition.scorekeeper.data"
            android:exported="false"
            android:syncable="false"/>
    </application>

</manifest>
